apiVersion: v1
kind: Service
metadata:
  name: car-repair-app-api
  namespace: ${namespace}
  labels:
    app: car-repair-app-api
spec:
  selector:
    app: car-repair-app-api
  ports:
    - name: http
      port: 80
      targetPort: 8080
  type: ClusterIP
