apiVersion: apps/v1
kind: Deployment
metadata:
  name: car-repair-app-api
  namespace: ${namespace}
  labels:
    app: car-repair-app-api
spec:
  replicas: ${api_replicas}
  selector:
    matchLabels:
      app: car-repair-app-api
  template:
    metadata:
      labels:
        app: car-repair-app-api
    spec:
      serviceAccountName: car-repair-app-api
      containers:
        - name: car-repair-app-api
          image: ${api_image}
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8080
          envFrom:
            - configMapRef:
                name: car-repair-app-config
            - secretRef:
                name: car-repair-app-secrets
          resources:
            requests:
              cpu: 250m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
          livenessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 30
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 15
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
