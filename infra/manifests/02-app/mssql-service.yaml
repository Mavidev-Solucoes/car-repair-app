apiVersion: v1
kind: Service
metadata:
  name: mssql
  namespace: ${namespace}
  labels:
    app: mssql
spec:
  selector:
    app: mssql
  ports:
    - name: mssql
      port: 1433
      targetPort: 1433
  type: ClusterIP
