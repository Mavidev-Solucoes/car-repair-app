apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../../base/gateway

  - swagger-ingress.yaml
patches:
  - path: cors-patch.yaml
  - path: delete-external-secret.yaml
