apiVersion: v1
kind: Service
metadata:
  name: car-repair-app
  labels:
    app.kubernetes.io/name: car-repair-app
    app.kubernetes.io/component: api
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: car-repair-app
    app.kubernetes.io/component: api
  ports:
    - name: http
      port: 80
      targetPort: 8080
